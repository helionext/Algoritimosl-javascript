/**
 * Criar um algoritimo que imprima uma tabela de conversão de polegadas para centimetros
 * . Deseja-se que na tabela conste valores desde 1 polegadas até 20 polegadas inteiras.
 */


for (let pol = 1; pol <= 20; pol++) {
    console.log(`${pol} polegadas equivale a ${pol * 2.54} centimetros`);   
}